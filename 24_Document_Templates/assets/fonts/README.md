# Fonts

Font files are loaded from `../../17_Website_Design_Docs/fonts/` via relative paths in the HTML templates.

If needed, fonts can also be loaded from the system font directory at `/Users/davisballard/Library/Fonts/`.
